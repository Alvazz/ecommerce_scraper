const createToken = () => {

};

const verifyToken = () => {

};

const hashPassword = (password) => {

};

const comparePassword = (password, hash) => {

};

module.exports = {
  createToken,
  verifyToken,
  hashPassword,
  comparePassword,
};
